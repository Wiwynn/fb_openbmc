#include <stdint.h>
#include <syslog.h>

void set_apml_mux_locked(uint8_t cpu) {
    (void)cpu;
}

void apml_mux_lock(uint8_t cpu) {
    (void)cpu;
}

void apml_mux_unlock(uint8_t cpu) {
    (void)cpu;
}
